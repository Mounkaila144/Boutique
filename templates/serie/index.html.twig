{% extends 'base.html.twig' %}

{% block title %}serie index{% endblock %}

{% block body %}
    <h2>
        Serie
    </h2>

    <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <div class="table-responsive pt-3">
                    <table class="table table-bordered">
                        <thead>
                        <tr>
                            <th>Id</th>
                            <th>Nom</th>
                            <th>Genre</th>
                            <th>Type</th>
                            <th>Modifier</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for serie in series %}
                            <tr>
                                <td>{{ serie.id }}</td>
                                <td>{{ serie.nom }}</td>
                                <td>{{ serie.genre }}</td>
                                <td>{{ serie.type ? serie.type|join(', ') : '' }}</td>
                                <td>{{ serie.saison }}</td>
                                <td>{{ serie.episode }}</td>
                                <td>
                                    <a href="{{ path('serie_edit', {'id': serie.id}) }}"><button type="submit" class="btn btn-primary me-2">Moifier</button></a>
                                </td>
                            </tr>
                        {% else %}
                            <tr>
                                <td colspan="7">no records found</td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>


    <a href="{{ path('serie_new') }}"><button type="submit" class="btn btn-primary me-2">Ajouter</button></a>
{% endblock %}
