{% extends 'user.html.twig' %}

{% block title %}Hello MainController!{% endblock %}

{% block body %}
        <div class="container film bg-black">
                <div class="bt row">

                </div>

                <div class="row">
                    {%for film in data.results %}
                        <div class="col">

                            <div class="card movie_card">

                                        <img src='{{ debut }}{{ film.poster_path }}' class="card-img-top" alt="...">
                                        <div class="card-img-bottom">
                                               <span class=""><a href="" class="link link-primary">{{ film.title }}</a></span>
                                        </div>

                                </div>

                        </div>
                    {% endfor%}
                    {{ knp_pagination_render(data) }}
                </div>
        </div>
{% endblock %}
