{% extends 'base.html.twig' %}

{% block title %}Video index{% endblock %}

{% block body %}
    <h2>
        Serie
    </h2>

    <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <div class="table-responsive pt-3">
                    <table class="table table-bordered">
                        <thead>
                        <tr>
                            <th>Id</th>
                            <th>Nom</th>
                            <th>Genre</th>
                            <th>Type</th>
                            <th>Saison</th>
                            <th>Episode</th>
                            <th>Modifier</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for video in videos %}
                            <tr>
                                <td>{{ video.id }}</td>
                                <td>{{ video.nom }}</td>
                                <td>{{ video.genre }}</td>
                                <td>{{ video.type ? video.type|join(', ') : '' }}</td>
                                <td>{{ video.saison }}</td>
                                <td>{{ video.episode }}</td>
                                <td>
                                    <a href="{{ path('video_edit', {'id': video.id}) }}"><button type="submit" class="btn btn-primary me-2">Moifier</button></a>
                                </td>
                            </tr>
                        {% else %}
                            <tr>
                                <td colspan="7">no records found</td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>


    <a href="{{ path('video_new') }}"><button type="submit" class="btn btn-primary me-2">Ajouter</button></a>
{% endblock %}
