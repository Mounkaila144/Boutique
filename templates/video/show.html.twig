{% extends 'base.html.twig' %}

{% block title %}Video{% endblock %}

{% block body %}
    <h1>Video</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ video.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ video.nom }}</td>
            </tr>
            <tr>
                <th>Genre</th>
                <td>{{ video.genre }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ video.type ? video.type|join(', ') : '' }}</td>
            </tr>
            <tr>
                <th>Saison</th>
                <td>{{ video.saison }}</td>
            </tr>
            <tr>
                <th>Episode</th>
                <td>{{ video.episode }}</td>
            </tr>
        </tbody>
    </table>
<a href="{{ path('video_index') }}"><button type="submit" class="btn btn-primary me-2">Moifier</button></a>
    Cancel
    <a href="{{ path('video_index') }}"><button class="btn btn-danger">retourt</button></a>
    {{ include('video/_delete_form.html.twig') }}
{% endblock %}
