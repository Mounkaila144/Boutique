{% extends 'base.html.twig' %}

{% block title %}Commande{% endblock %}

{% block body %}
    <h1>Commande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande.id }}</td>
            </tr>

            <tr>
                <th>Panier</th>
                <td>
                    {% for panier in commande.panier.items %}
                        <ul>
                            <li>{{ panier.quantity }} {{panier.nom}} {{ panier.price }} CFA</li>
                        </ul>
                    {% endfor %}
                </td>
            </tr>
            <tr>
                <th>Total</th>
                <td>{{ commande.panier.cartTotal }}</td>
            </tr>

        </tbody>
    </table>

    <a href="{{ path('app_panier_index') }}">back to list</a>

    <a href="{{ path('app_panier_edit', {'id': commande.id}) }}">edit</a>

    {{ include('panier/_delete_form.html.twig') }}
{% endblock %}
